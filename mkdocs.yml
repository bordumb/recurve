site_name: recurve
site_description: Claims-driven recursive software improvement — point it at a repo or a PRD and burn down the gap between claimed and proven.
repo_url: https://github.com/bordumb/recurve
repo_name: bordumb/recurve

# Internal repo documents that must NOT be published or indexed on the site.
# plans/ holds the design spec (plan.md), the PRD (improvements.md), and the
# internal design docs — the designated provenance zone, in-repo only.
# research/ holds internal market/competitive notes — local-only (also gitignored).
exclude_docs: |
  plans/
  research/

theme:
  name: material
  palette:
    # Dark Mode (default)
    - scheme: slate
      primary: black
      accent: indigo
      toggle:
        icon: material/weather-night
        name: Switch to light mode
    # Light Mode
    - scheme: default
      primary: white
      accent: indigo
      toggle:
        icon: material/weather-sunny
        name: Switch to dark mode
  font:
    text: Inter
    code: JetBrains Mono
  icon:
    repo: fontawesome/brands/github
  features:
    - navigation.tabs          # persistent top navbar — visible on the hero too
    - navigation.tabs.sticky   # keep the tabs pinned while scrolling
    - navigation.instant
    - navigation.tracking
    - navigation.sections
    - navigation.path
    - toc.follow
    - search.suggest
    - search.highlight
    - content.code.copy

extra_css:
  - stylesheets/extra.css

plugins:
  - search

markdown_extensions:
  - admonition
  - pymdownx.details
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format
  - pymdownx.tabbed:
      alternate_style: true
  - attr_list
  - md_in_html               # grid cards + the centered hero <div markdown>
  - pymdownx.emoji:          # :material-*: icons on the landing page
      emoji_index: !!python/name:material.extensions.emoji.twemoji
      emoji_generator: !!python/name:material.extensions.emoji.to_svg
  - pymdownx.highlight:
      anchor_linenums: true
  - pymdownx.inlinehilite
  - def_list
  - tables
  - toc:
      permalink: true
      toc_depth: 3

nav:
  - Home: index.md
  - About: about.md
  - User guide:
      - Installation: install.md
      - Usage:
          - Claude skill: usage-claude-skill.md
          - Provider-agnostic: usage-provider-agnostic.md
      - Hardening your probes: hardening.md
      - Run data & trajectories: run-data.md
      - Multi-repo: multi-repo.md
      - Claim packs: packs.md
  - Developer guide:
      - The verification layer: verification-layer.md
      - Evidence & receipts: evidence.md
      - Architecture: architecture.md
