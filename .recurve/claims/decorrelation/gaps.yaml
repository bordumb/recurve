# gaps.yaml — the live ledger: verified observations only.
# Entries arrive here through `baseline` (the promotion ceremony); the
# `observed` field quotes actual dated output, never a prediction.

- id: DC-1
  title: 'oracle tier: recorded, derived, rendered — and honest about what backs it'
  class: missing-surface
  status: closed
  severity: headline
  reads: none
  covers:
  - DC-1
  evidence:
  - recurvelib/analysis/oracle_tier.py
  observed: 'GREEN at baseline 2026-07-05: oracle tier is recorded, derived from real evidence
    (never hand-set), and rendered in ledger + show; the mechanical/LLM reference split is
    content-verified'
  smallest_fix: recurvelib.analysis.oracle_tier derives OracleTier from a gap's trap/reference
    shape plus a tier-evidence log; Gap.parse refuses a hand-set tier field; render.py/ledger.py/show.py
    surface it.
  probe: probes/dc-1.sh
  unlocks: a GREEN's ledger entry says what kind of evidence backed it, not just that it passed

- id: DC-2
  title: surface the weak-oracle question during authoring, not after
  class: missing-surface
  status: closed
  severity: feature
  reads: none
  covers:
  - DC-2
  evidence:
  - recurvelib/analysis/oracle_tier.py
  observed: 'GREEN at baseline 2026-07-05: the weak-oracle question surfaces at authoring/ledger
    time — advisory only, never blocking GREEN — and resists reference-field-without-a-real-pass
    gaming'
  smallest_fix: recurvelib.analysis.oracle_tier.needs_oracle_advisory flags a closed claim
    resting on self_probe/trap_hardened alone (no reference, no adversary evidence, no oracle_waiver);
    recurve ledger renders it as a visible, non-blocking footer.
  probe: probes/dc-2.sh
  unlocks: a claim closing on a weak oracle is visible at ledger time, not discovered later

- id: DC-3
  title: the O6-incident regression fixture, at claim level
  class: missing-surface
  status: closed
  severity: headline
  reads: none
  covers:
  - DC-3
  evidence:
  - eval/runs/o6/results.jsonl
  - recurvelib/adapters/adversary/cross_model.py
  observed: 'GREEN at baseline 2026-07-05: the O6 claim-level replay is caught: a same-model
    self-authored probe alone says GREEN, but the real cross_model adversary disagrees, and
    its proposed counterexample is validated by the EXISTING capture() rule as genuine evidence
    — not a silent pass, not noise'
  smallest_fix: a toy claim replaying the O6 bug class (attempted-vs-succeeded misreading);
    the real cross_model adversary disagrees with the same-model self-authored probe, and
    capture() validates its proposed counterexample as genuine.
  probe: probes/dc-3.sh
  unlocks: R2''s cross-model adversary is proven against the actual incident it was built
    to catch, not just a hypothetical
