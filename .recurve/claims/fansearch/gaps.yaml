# gaps.yaml — the live ledger: verified observations only.
# Entries arrive here through `baseline` (the promotion ceremony); the
# `observed` field quotes actual dated output, never a prediction.

- id: FS-1
  title: the proxy sanity check recognizes real dissipative structure
  class: missing-surface
  status: closed
  severity: headline
  reads: none
  covers:
  - FS-1
  observed: 'GREEN at baseline 2026-07-06: GREEN: dphi_dt matches the closed-form identity
    on 7 vectors; dissipative avg score 1.000, worst violating score 0.000'
  smallest_fix: add probes/fs-1_impl/proxy_sanity.py exposing dphi_dt(nu, alpha, gamma, N,
    u) and score(nu, alpha, gamma, N, u) that compute the shell-model weighted-energy derivative
    via the closed-form telescoping identity (lam=2), correct against the fixed vector table
    probes/fs-1.sh checks.
  probe: probes/fs-1.sh
  unlocks: every later fan-out claim -- the discovery engine is not worth building until this
    scorer can tell real dissipative structure from an impostor.

- id: FS-2
  title: the ProxyEvaluator port resolves through the generalized registry, never a fourth
    hand-copy
  class: missing-surface
  status: closed
  severity: feature
  reads: none
  covers:
  - FS-2
  observed: 'GREEN at baseline 2026-07-06: GREEN: ProxyEvaluator seam resolves through the
    generic registry; off proxy scores validly; malformed/unknown adapters refused; no fourth
    hand-copy in registry.py'
  smallest_fix: add recurvelib/core/protocols.py (ProxyEvaluator, ProxyScore), a generic build_registry/resolve
    pair in recurvelib/adapters/registry.py, recurvelib/adapters/proxy/ (PROXY_ADAPTERS with
    a trivial off adapter), and a [fansearch] proxy config knob defaulting to off.
  probe: probes/fs-2.sh
  unlocks: the seam a real domain proxy (F5) registers against, without a fourth hand-copy
    of the adversary/governor/boundary registry pattern.

- id: FS-3
  title: the dyadic_lyapunov proxy is registered, deterministic, and correct against the verified
    closed form
  class: missing-surface
  status: closed
  severity: feature
  reads: none
  covers:
  - FS-3
  observed: 'GREEN at baseline 2026-07-06: GREEN: dphi_dt matches the verified closed form
    on 7 vectors; malformed candidates refused; registered and deterministic'
  smallest_fix: add recurvelib/adapters/proxy/dyadic_lyapunov.py (Candidate, dphi_dt, DyadicLyapunovProxy)
    computing the general (not single-shell-restricted) weighted-energy derivative directly
    from the shell RHS, and register it in PROXY_ADAPTERS as "dyadic_lyapunov".
  probe: probes/fs-3.sh
  unlocks: the real proxy a classical-optimizer-driven campaign scores candidates with, in
    place of the throwaway Stage 0-2 scripts.
