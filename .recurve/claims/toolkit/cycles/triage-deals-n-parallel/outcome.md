# Outcome: triage-deals-n-parallel

Closed TK-7. Sculpt: `lanes()` in recurvelib/triage.py (per-suite top,
value-first, parked-aware) + `next --lanes N [--json]` in the CLI. Gate:
probe GREEN, 7/7 traps RED, fleet gate green, coverage green. Promoted with
prose rewritten to the new reality.
