# gaps.yaml — the live ledger: verified observations only.
# Entries arrive here through `baseline` (the promotion ceremony); the
# `observed` field quotes actual dated output, never a prediction.

- id: PL-1
  title: recurve decide surfaces the stopping controller (verdict_for mirrors controller.decide)
  class: missing-surface
  status: closed
  severity: feature
  reads: none
  covers:
  - PL-1
  evidence:
  - recurvelib/decide_cli.py
  - recurvelib/cli.py
  observed: 'GREEN at baseline 2026-07-01: recurve decide surfaces the controller faithfully:
    verdict_for mirrors controller.decide'
  smallest_fix: add recurvelib/decide_cli.py with verdict_for(open, regressed, broken, uncovered,
    divergent) returning controller.decide([Progress(...)]).value; add cmd_decide in cli.py
    that parses a progress vector from flags and prints verdict_for(...), register the `decide`
    subparser, and add it to the module docstring usage list
  probe: probes/pl-1.sh
  unlocks: 'the loop asking recurve for its stop verdict from a measured vector (controller
    wired) — the first step of #4, and the first #4 claim burned down BY the loop'

- id: PL-2
  title: recurve frontier surfaces the completeness frontier (frontier_ids mirrors compute_frontier)
  class: missing-surface
  status: closed
  severity: feature
  reads: none
  covers:
  - PL-2
  evidence:
  - recurvelib/frontier_cli.py
  - recurvelib/cli.py
  observed: 'GREEN at baseline 2026-07-01: recurve frontier surfaces the frontier faithfully:
    frontier_ids mirrors compute_frontier'
  smallest_fix: add recurvelib/frontier_cli.py with frontier_ids(surface, covered_ids, deferred_ids=())
    returning [p.id for p in compute_frontier(surface, covered_ids, deferred_ids).frontier];
    add cmd_frontier in cli.py that reports the frontier, register the `frontier` subparser,
    and add it to the module docstring usage list
  probe: probes/pl-2.sh
  unlocks: the loop seeing what no claim covers (completeness surfaced) — the signal it needs
    to refuse STOP-SUCCESS while the frontier is nonempty

- id: PL-3
  title: the burndown loop's stop decision consults controller.decide (not the cap watchdog)
  class: missing-surface
  status: open
  severity: feature
  reads: none
  covers:
  - PL-3
  observed: 'RED at baseline 2026-07-01: ours=burndown.sh never calls the decide verb oracle=the
    loop asks controller.decide for its stop verdict'
  smallest_fix: burndown.sh computes the cycle's gate vector (open/regressed/broken parsed
    from recurve matrix) and calls `{{PROG}} decide` with it; the success-halt is gated on
    the verdict being STOP-SUCCESS, so controller.decide decides when the loop is done — not
    the ad-hoc no-work/cap watchdog. Must keep every TK burndown-behavior claim green.
  probe: probes/pl-3.sh
  unlocks: 'the loop''s stop verdict coming from controller.decide — the core of #4, the verification
    layer wired into the running loop (uncovered/divergent join the vector as PL-4 lands)'
