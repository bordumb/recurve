# gaps.yaml — the live ledger: verified observations only.
# Entries arrive here through `baseline` (the promotion ceremony); the
# `observed` field quotes actual dated output, never a prediction.

- id: TK-1
  title: no origin vocabulary ships in the engine, CLI, schemas, templates, or packs
  class: staging
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-1
  evidence:
  - acceptance/provenance.sh:1
  observed: 'GREEN at baseline 2026-06-12: shipped surface carries no origin vocabulary'
  smallest_fix: keep provenance confined to plan.md's designated zones and the acceptance
    dir
  probe: probes/tk-1.sh
  unlocks: installing recurve anywhere without importing its first customer

- id: TK-2
  title: live-equivalent to both ancestor instances on every shared command
  class: wire-mismatch
  status: closed
  severity: headline
  reads: none
  covers:
  - TK-2
  evidence:
  - acceptance/diff.sh:1
  observed: 'GREEN at baseline 2026-06-12: live-equivalent to both ancestors (fast subset)'
  smallest_fix: (already conformant at extraction) — guard the equivalence forever
  probe: probes/tk-2.sh
  unlocks: the migration IS the test suite; if this breaks, the engine generalized wrong

- id: TK-3
  title: 'the verdict map is total: crash/signal/timeout coerce to BROKEN, never a verdict'
  class: wire-mismatch
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-3
  evidence:
  - recurvelib/probe.py:1
  observed: 'GREEN at baseline 2026-06-12: verdict map total; records and receipts validate'
  smallest_fix: keep the {0,1}→verdict map closed; everything else is BROKEN
  probe: probes/tk-3.sh
  unlocks: no fourth state — a segfault can never bless or damn a claim

- id: TK-4
  title: a second loop on a locked tree is refused, never admitted
  class: staging
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-4
  evidence:
  - recurvelib/lock.py:1
  observed: 'GREEN at baseline 2026-06-12: second loop refused while the first holds the tree'
  smallest_fix: O_EXCL lock keyed by resolved tree path; steal is human-only
  probe: probes/tk-4.sh
  unlocks: suites sharing a tree federate into one gate instead of corrupting each other

- id: TK-5
  title: validate refuses an unfalsified probe (no trap fixture, no waiver)
  class: missing-surface
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-5
  evidence:
  - recurvelib/cli.py:1
  observed: 'GREEN at baseline 2026-06-12: unfalsified probe rejected by validate'
  smallest_fix: trap discipline enforced at validate; waivers are counted, listed debt
  probe: probes/tk-5.sh
  unlocks: a probe that has never been seen RED can never silently guard nothing

- id: TK-6
  title: a receipt edited after the fact fails validation loudly
  class: wire-mismatch
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-6
  evidence:
  - recurvelib/records.py:1
  observed: 'GREEN at baseline 2026-06-12: tampered receipt rejected — the evidence is tamper-evident'
  smallest_fix: self-hash over canonical content; the chain breaks visibly or it is not a
    chain
  probe: probes/tk-6.sh
  unlocks: code accompanied by its evidence, checkable by someone who was not there

- id: TK-7
  title: triage deals N parallel lanes from pairwise-disjoint suites
  class: missing-surface
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-7
  evidence:
  - recurvelib/triage.py:1
  observed: 'GREEN at cycle triage-deals-n-parallel 2026-06-12: lanes dealt from disjoint suites, value-first within each'
  smallest_fix: add lanes() to triage (top gap per suite, value-first) and `next --json --lanes
    N`
  probe: probes/tk-7.sh
  unlocks: parallel lanes that can never sculpt the same ledger or prose

- id: TK-8
  title: an orchestrator can hold the tree lock across CLI invocations
  class: missing-surface
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-8
  evidence:
  - recurvelib/cli.py:1
  observed: 'RED at baseline 2026-06-12: ours=no acquire surface oracle=lock acquire/release
    for orchestrators'
  smallest_fix: add `lock acquire` (refused while held) and `lock release` beside status/steal
  probe: probes/tk-8.sh
  unlocks: a shell orchestrator that spans many commands can still be the single loop on the
    tree

- id: TK-9
  title: parallel lanes land through one gated serialization point
  class: missing-surface
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-9
  evidence:
  - templates/workflows/burndown.js:1
  observed: 'RED at baseline 2026-06-12: ours=no parallel loop oracle=worktree lanes + gated
    serialization (plan §15.9)'
  smallest_fix: ship workflows/burndown-parallel.sh — worktree lanes over disjoint suites;
    per-landing gap-GREEN + fleet-gate check; revert-and-discard on failure; per-landing commit
  probe: probes/tk-9.sh
  unlocks: wall-clock scales with lanes while the ratchet stays single-file — the v2 contract,
    proven

- id: TK-10
  title: 'the unarmed backlog is visible: next --json reports drafts and pending adjudications'
  class: missing-surface
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-10
  evidence:
  - recurvelib/cli.py:1
  observed: 'GREEN at baseline 2026-06-13: drafts and pending adjudications visible to orchestrators'
  smallest_fix: next --json carries a drafts list (suite, pending count) and an adjudications_pending
    count read from gaps.draft.yaml and ADJUDICATE.md
  probe: probes/tk-10.sh
  unlocks: loops that arm their own next wave instead of stranding the draft backlog

- id: TK-11
  title: 'record append is idempotent: one cycle, one journal line, however many appenders'
  class: wire-mismatch
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-11
  evidence:
  - recurvelib/cli.py:1
  observed: 'GREEN at baseline 2026-06-13: one record appended twice lands once'
  smallest_fix: cmd_record skips an append whose canonical serialization already exists in
    records.jsonl
  probe: probes/tk-11.sh
  unlocks: triage priors and cost prediction that do not double-count cycles

- id: TK-12
  title: the burndown loops arm the next wave instead of stranding a draft backlog
  class: missing-surface
  status: closed
  severity: headline
  reads: none
  covers:
  - TK-12
  evidence:
  - templates/workflows/burndown.sh:1
  observed: 'GREEN at baseline 2026-06-13: burndown templates arm or surface the draft backlog'
  smallest_fix: serial loop gains an arm_wave stage (author probes for a draft batch, run
    baseline, continue; bounded by ARM_WAVES, refused while forks pend); parallel and orchestrator
    twins surface the unarmed backlog at halt
  probe: probes/tk-12.sh
  unlocks: one launch burns down the whole spec, wave after wave, with humans only at forks
    and review gates

- id: TK-13
  title: report renders the deterministic run dataset with no narrator and no network
  class: missing-surface
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-13
  evidence:
  - recurvelib/report.py:1
  observed: 'GREEN at baseline 2026-06-13: deterministic run dataset rendered from records+ledger+git;
    the seeded TODO was counted'
  smallest_fix: add recurvelib/report.py + a `report` subcommand — progress from the ledger,
    cycle durations and an ETA projection from records.jsonl, and a diff honesty scan over
    the git range the records cover
  probe: probes/tk-13.sh
  unlocks: a run dataset a maintainer reads before signing, free with every cycle

- id: TK-14
  title: --narrate appends narrator prose without ever owning the report
  class: missing-surface
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-14
  evidence:
  - recurvelib/report.py:1
  observed: 'GREEN at baseline 2026-06-13: narrator prose appended; missing config fails clean;
    a dying narrator never costs the report'
  smallest_fix: '`report --narrate` pipes the rendered report + a JSON array of the cycle
    records to [report] narrator on stdin and appends its stdout; no narrator configured is
    a clean exit 2; a failing/timed-out narrator costs the prose only (exit 1)'
  probe: probes/tk-14.sh
  unlocks: LLM judgment over the run, priced by the narrator, with the numbers always free

- id: TK-15
  title: the stamped burndown workflow carries no sandbox-forbidden runtime call
    and resolves its paths from an absolute root
  class: wire-mismatch
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-15
  evidence:
  - templates/workflows/burndown.js:1
  - recurvelib/init.py:1
  observed: 'GREEN at baseline 2026-06-14: a freshly stamped burndown.js carries no Date.now/Math.random/new
    Date call and reads ${ROOT}/.recurve/RUN.md from an absolute const ROOT'
  smallest_fix: burndown.js uses a deterministic RUN_ID (no Date.now), stamps an absolute
    `const ROOT = '{{ROOT}}'`, and its agent prompts read ${ROOT}/.recurve/RUN.md + write
    ${ROOT}/.recurve/state/reports/...; init.py adds ROOT=str(target) to the template subs
  probe: probes/tk-15.sh
  unlocks: an unattended burndown that resumes deterministically and resolves its paths
    regardless of the orchestrator's launching cwd

- id: TK-16
  title: matrix --gate federates each sculpt's own gate command; a single-tree config
    is unchanged
  class: missing-surface
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-16
  evidence:
  - recurvelib/cli.py:188
  - recurvelib/config.py:67
  observed: 'GREEN at baseline 2026-06-14: a config with [sculpts.x] gate = "false" makes
    matrix --gate exit non-zero, gate = "true" passes, and a no-[sculpts.*] config gates
    as before'
  smallest_fix: cmd_matrix's --gate path iterates cfg.sculpts, runs each non-empty gate
    via subprocess (cwd = sculpt tree dir), prints one line per sculpt, and ANDs each
    exit==0 into gate_ok; config.py parses [sculpts.<name>] into SculptConfig
  probe: probes/tk-16.sh
  unlocks: the first-class scaffold-sculpts-platform feedback loop — one loop gates a
    target's probes AND every secondary tree's own gate

- id: TK-17
  title: the stamped in-session loop skill spawns a fresh sub-agent per cycle, locks, and
    gates
  class: missing-surface
  status: closed
  severity: feature
  reads: none
  covers:
  - TK-17
  evidence:
  - templates/skills/loop.md
  - recurvelib/init.py:295
  observed: 'GREEN at baseline 2026-07-01: stamped loop skill spawns a fresh sub-agent per
    cycle, acquires the lock, and gates on matrix --gate'
  smallest_fix: templates/skills/loop.md drives the loop in-session as the orchestrator, spawning
    one fresh sub-agent per cycle (the ledger is the only memory), acquiring the tree lock,
    and gating on matrix --gate; init.py stamps skills/loop.md into .claude/skills/loop/SKILL.md
    alongside burndown/cycle/review
  probe: probes/tk-17.sh
  unlocks: the in-session loop as a first-class surface (the primary entry point of the command-surface
    plan) without losing the clean-agent-per-cycle hygiene
