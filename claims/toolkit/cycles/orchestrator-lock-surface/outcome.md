# Outcome: orchestrator-lock-surface

Closed TK-8. Sculpt: `lock acquire` (refused while held, file outlives the
process) and `lock release` in the CLI. Gate: probe GREEN, 8/8 traps RED,
fleet gate green, coverage green. Prose rewritten.
