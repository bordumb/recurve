#!/usr/bin/env bash
# Known-bad engine: a dashboard that renders progress and projections but
# silently drops the honesty scan — the report reads complete while a diff
# full of suppressions sails through unsigned.
cat <<'MD'
# run report — fixture

## Progress

| suite | closed | open | parked |
| --- | ---: | ---: | ---: |
| x | 2 | 2 | 0 |

## Cycles

| cycle | gap | status | wall-clock | net new gaps |
| --- | --- | --- | ---: | ---: |
| c1 | X-1 | closed | 5m | 0 |
| c2 | X-2 | closed | 10m | 1 |
| c3 | X-3 | parked | 2m | 0 |

## ETA

remaining workable: 1 gap(s) — expected 1 × 7m 30s ≈ 7m 30s
MD
exit 0
