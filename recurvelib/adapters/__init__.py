"""Concrete adapters for the `Adversary`/`Governor` ports (`recurvelib.loop.reviewers`).

Mirrors `core`/`analysis`/`io`/`loop` — a sibling top-level package, so the
loop never imports `subprocess`/docker/an identity library directly (see
`docs/plans/ablation-infra.md`).
"""
